import { AppHiglightDirective } from './app-higlight.directive';

describe('AppHiglightDirective', () => {
  it('should create an instance', () => {
    const directive = new AppHiglightDirective();
    expect(directive).toBeTruthy();
  });
});
